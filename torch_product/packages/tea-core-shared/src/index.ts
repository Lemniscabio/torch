// @torch/core-shared — public surface of the calculation engine.
// Types + bounds + defaults + form-facing helpers only. No math.
// Safe to bundle into the static frontend export.
export * from "./types";
export * from "./constants";
